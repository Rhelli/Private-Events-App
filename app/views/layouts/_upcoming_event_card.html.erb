<ul>
  <% @upcoming_events.each do |event| %>
  <div class="event-card">
    <li>
      <h2><%= link_to(event.name, event_path(event))%></h2>
      <div>
        <p>Created <%= time_ago_in_words(event.created_at)%> ago</p>
        <p>Host: <%= event.creator.name.capitalize %></p>
      </div>
    </li>
  </div>
  <% end %>
</ul>